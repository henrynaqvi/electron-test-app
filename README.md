
### Quick Start

```bash
$ npm i # installs node modules
$ npm run dist:win # create build
$ npm run dev:electron # runs electron app in dev mode.
$ npm run dev:react  # run locally in browser i.e : http://localhost:3000
```
